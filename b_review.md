# Part B: Code Explanation & Code Review

## 1. คำอธิบายโค้ด d_homework.js (ทีละบรรทัด)

| บรรทัดโค้ด                    | ทำอะไร                                                                                                       |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `interface Bus {`             | สร้าง **interface** ชื่อ `Bus` เพื่อกำหนดว่า object ของรถบัสควรมีข้อมูลอะไรและเป็นชนิดไหน                    |
| `route: string;`              | กำหนดว่า `route` ต้องเป็นข้อความ (`string`)                                                                  |
| `passengers: number;`         | กำหนดว่า `passengers` ต้องเป็นตัวเลข (`number`)                                                              |
| `late: boolean;`              | กำหนดว่า `late` ต้องเป็นค่า `true` หรือ `false` (`boolean`)                                                  |
| `const bus: Bus = {`          | สร้าง object ชื่อ `bus` และบอก TypeScript ว่า object นี้ต้องทำตามรูปแบบ `Bus`                                |
| `route: "NGV-1",`             | กำหนดเส้นทางเป็น `"NGV-1"` ซึ่งเป็น `string`                                                                 |
| `passengers: 45,`             | กำหนดจำนวนผู้โดยสารเป็น `45` ซึ่งเป็น `number`                                                               |
| `late: false,`                | กำหนดว่ารถมาสายหรือไม่ โดย `false` หมายถึงไม่สาย                                                             |
| `bus.passengers = "เยอะมาก";` | พยายามเปลี่ยน `passengers` เป็น `string` แต่ `Bus` กำหนดไว้ว่าต้องเป็น `number` จึงถูก TypeScript ขีดเส้นแดง |
| `}`                           | ปิด object `bus`                                                                                             |


## 2. การรีวิวโค้ดจาก AI (ในฐานะ Senior Developer)

### ข้อแนะนำที่เลือกนำมาปรับแก้:
- **ข้อที่เลือก:** ข้อ 1 แยกการตรวจสอบ NaN และ Infinity ด้วย `Number.isFinite()`
- **เหตุผลที่เลือก:** เห็นด้วยอย่างยิ่งเพราะใน JavaScript ค่า `typeof NaN` จะได้ผลลัพธ์เป็น `"number"` การเช็กแค่ `typeof` แบบเดิมจึงยังมีจุดบกพร่องอยู่ การเปลี่ยนมาใช้ `Number.isFinite()` ช่วยป้องกัน Edge Cases ได้รัดกุมและปลอดภัยขึ้นมาก
- **การเปลี่ยนแปลงในโค้ด:** ปรับเงื่อนไขการตรวจสอบ Input ด้านบนสุดของฟังก์ชันเป็น `if (!Number.isFinite(distanceKm) || distanceKm < 0)`

### ข้อแนะนำที่ไม่เลือก:
- **ข้อที่ไม่เลือก:** ข้อ 2 (การปรับโครงสร้าง/ลดตัวแปร) และ ข้อ 3 (เพิ่ม Test Cases)
- **เหตุผลที่ไม่เลือก:** 
  - ข้อ 2: โครงสร้างเดิมใช้ `Math.ceil(distanceKm - 2)` โดยตรง อ่านเข้าใจง่ายเพียงพอแล้วสำหรับ Scope โปรเจกต์นี้
  - ข้อ 3: โจทย์ Lab กำหนดให้ทดสอบ 3 กรณีเฉพาะ (1.5, 2, 7.2) การเพิ่ม Test Cases นอกเหนือจากนี้อาจทำให้การตรวจข้อสอบด้วยระบบอัตโนมัติหรือความสอดคล้องกับเพื่อนร่วมชั้นคลาดเคลื่อนได้