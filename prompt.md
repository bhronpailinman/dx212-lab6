## Part A: Prompt แย่ vs Prompt ดี

### Prompt A1 (ตั้งใจให้แย่)
- **Prompt ที่ใช้:** "เขียนฟังก์ชันคำนวณค่ารถด้วย JavaScript ให้หน่อย"
- **ผลลัพธ์ย่อ (เอาโค้ดสั้นๆ อันแรกที่คุณได้มาวางตรงนี้):**
  ```javascript
  function calculateFare(distance, pricePerKm) {
      let fare = distance * pricePerKm;
      return fare;
  }
  let total = calculateFare(10, 5);
  console.log(total);

## Part B — Code Review
- **Prompt ที่ใช้อธิบาย:** "อธิบายโค้ดนี้ทีละบรรทัดให้นักศึกษาปี 2..."
- **การตรวจคำอธิบาย:** ตรวจแล้วถูก
- **Prompt ที่ใช้รีวิว:** "รีวิวโค้ดนี้ในฐานะ senior developer: บอกจุดที่ควรปรับปรุง 3 ข้อ เรียงตามความสำคัญ พร้อมเหตุผลและโค้ดที่แก้แล้ว"
- **ข้อแนะนำที่เลือก:** ใช้ `Number.isFinite()` เช็กค่า NaN/Infinity ตามคำแนะนำข้อที่ 1
- **ข้อที่ไม่เลือก:** ข้อ 2 และ 3 เนื่องจากโครงสร้างเดิมอ่านง่ายอยู่แล้ว และต้องการรักษา Test Cases ตรงตามที่โจทย์ระบุ

## Part C — Debugging
- **Prompt ที่ใช้:** [วาง Prompt สูตรดีบัก 4 อย่างจาก Step 3]
- **สิ่งที่เดาไว้:** 
  1. Arrow function ใน filter ขาด return
  2. reduce ขาด Initial value (0)
- **เดาถูกกี่จุด:** ถูกทั้ง 2 จุด
- **ผลลัพธ์หลังแก้ไข:** รัน node c_debug.js ได้สายที่มาสายเป็น ["NGV-2", "NGV-3"] และผู้โดยสารรวมเป็น 145 ตรงตามคาดหวัง

### Checkpoint C: สาเหตุของ Bug
1. **Arrow Function กับปีกกา `{}`:** ใน JavaScript ถ้าเขียน Arrow Function แบบมีปีกกา `{}` (Block body) ต้องใส่คีย์เวิร์ด `return` เสมอ มิฉะนั้นฟังก์ชันจะ คืนค่าเป็น `undefined` (แก้โดยการถอดปีกกาออกเป็น Implicit return `b => b.late` หรือใส่ `return b.late`)
2. **Initial Value ใน `.reduce()`:** หากไม่ใส่ค่าเริ่มต้น ตัว `.reduce()` จะนำ Element ตัวแรกสุดของ Array (ซึ่งเป็น Object `{ route: "NGV-1", ... }`) มาใช้เป็นค่าเริ่มต้นของ `sum` ทำให้เกิดการนำ Object ไปบวกกับ Number เกิดเป็นข้อความ `[object Object]6238` (แก้โดยใส่ `0` เป็น Initial value)

## Part D — User Story to Code
- **User Story:** "ในฐานะคนที่เดินทางบ่อย อยากดาวน์โหลดเพลงไว้ฟังออฟไลน์ เพื่อประหยัดเน็ตมือถือ แสดงสัญลักษณ์สถานะดาวน์โหลด และเล่นเพลงได้แม้นำเครื่องออกนอกเครือข่าย"
- **3 คำถามที่ AI ถามก่อนเขียนโค้ด:**
  1. โครงสร้างข้อมูลเพลงควรเก็บสถานะดาวน์โหลดอย่างไร? (ตอบ: ใช้ boolean `isDownloaded`)
  2. กรณีออฟไลน์แล้วกดเล่นเพลงที่ไม่ได้ดาวน์โหลดจะเกิดอะไรขึ้น? (ตอบ: แสดง Error แจ้งเตือน)
  3. ต้องรองรับการตรวจสอบข้อมูลขาเข้า (Validation) หรือไม่? (ตอบ: ต้องรองรับ)
- **Prompt ครบ 5 ส่วนที่ใช้:** [วาง Prompt จาก Step 4]
- **ผลการรันใน node d_story.js:** ผ่านครบทั้ง 3 กรณี

---

### Checkpoint D: สรุปบทเรียน User Story
- **ความสำคัญของการให้ AI ถามคำถามก่อน:** ช่วยดึงรายละเอียดขอบเขต (Edge Cases) และสถานะการทำงานของระบบที่ User Story ยังระบุไม่ชัดเจนออกมา ทำให้ได้ Prompt ที่รัดกุมและเขียนโค้ดตรงตามความต้องการตั้งแต่รอบแรกโดยไม่ต้องรัน Iterate หลายรอบ